
import { Sale, Product, Customer } from '../types';

// Gemini API devre dışı bırakıldı.
export const generateBusinessInsights = async (
  sales: Sale[],
  products: Product[],
  customers: Customer[]
): Promise<string> => {
  return "Yapay zeka özelliği devre dışı bırakıldı.";
};
